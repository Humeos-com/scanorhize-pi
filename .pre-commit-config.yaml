repos:
  - repo: local
    hooks:
      - id: bump-version
        name: Bump version if Python files changed
        entry: python bump_version.py
        language: system
        files: '\.py$'
        pass_filenames: false
        stages: [pre-commit]

